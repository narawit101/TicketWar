{
  "name": "ticketwar-backend",
  "version": "1.0.0",
  "description": "TicketWar WebSocket & Realtime Server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch --env-file-if-exists=../.env --env-file-if-exists=.env server.js"
  },
  "dependencies": {
    "socket.io": "^4.8.3"
  }
}
